#  py -m pip install dotenv

# use {py -m pip install --upgrade(or -U) <package_name>} to update packages


# use {py -m venv <virtual_environment_name (starting with a dot)>}


# To activate the virtual environment
# for windows (CMD / PowerShell) => use { .venv/scripts/activate}

# use {source .venv/scripts/activate} while using Git-bash


# this command activates the virtual environment
# to de-activate .venv , just type (deactivate)

# <pip list> or <py -m pip list> to list all the packages installed

# packages in global environment are completely different from the packages in virtual environment

# pypi.org for finding python packages

# pip freeze > requirements.txt  (this command is used to create a requirements file , that includes all the required packages and dependencies)

# create a .gitignore file to (ignore some credential files being pushed on github)
# add .venv and .env to the (.gitignore) file

# link ntag for Flask(python) =>
# <link rel="stylesheet" href="{{ url_for('static',filename='styles/style.css')}}">

